# Rules

Read before repo work.

- Accuracy first; use the smallest relevant files/slices.
- Inspect before edits; preserve user changes.
- Secrets stay in env/local files; commit examples only; update `.gitignore` first.
- New rules must be short, trigger-based, and listed below.

Read next when matching:
- Code change: `.codex/code-rules.md`
- Git: `.codex/git-workflow.md`
