# Schedly FE

Flutter client for Schedly.

## Run

```bash
flutter pub get
flutter run -d chrome
```

## Verify

```bash
flutter test
flutter analyze
flutter build web
flutter build apk --debug
flutter build macos --debug
```
